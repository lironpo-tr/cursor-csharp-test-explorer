# C# Test Explorer for Cursor

Discover, run, and debug C# tests directly from a dedicated sidebar. Supports **NUnit**, **xUnit**, and **MSTest** — any framework compatible with `dotnet test`.

## Features

- **Test Discovery** — Automatically scans your workspace for test projects and builds a hierarchical tree view (Project → Namespace → Class → Method)
- **Run Tests** — Run all tests, a single project, a namespace, a class, or an individual test method
- **Multi-Select** — Select multiple nodes in the tree and run them together
- **Pass / Fail Icons** — Green check, red X, and skip indicators in the tree
- **Error Details** — Failed tests show error messages and stack traces inline
- **Source Navigation** — Click a test to jump to its source file and line
- **Debug Tests** — Debug any test node with the `coreclr` debugger attached
- **Auto-Refresh** — Re-discovers tests when `.cs` or `.csproj` files change
- **Filter** — Search tests by name with the sidebar filter bar
- **Stop Run** — Cancel an in-progress test run from the view toolbar

## Getting Started

1. Open a workspace that contains one or more C# test projects
2. The extension activates automatically when it detects `.csproj` files
3. Open the **C# Tests** sidebar (beaker icon in the Activity Bar)
4. Click **Refresh Tests** to discover your tests
5. Run or debug tests using the inline buttons or the right-click context menu

## Test Status Icons

Each test node in the sidebar displays an icon reflecting its current state:

| Icon | Meaning |
|------|---------|
| $(pass) Green checkmark | **Passed** — test executed successfully |
| $(error) Red circle | **Failed** — test threw an error or assertion failure |
| $(debug-step-over) Yellow arrow | **Skipped** — test was ignored or filtered out |
| $(loading~spin) Spinner | **Running** — test execution is in progress |
| $(circle-outline) Empty circle | **Not run** — test has been discovered but not yet executed |

Parent nodes (projects, namespaces, classes) aggregate the state of their children: if any child fails the parent shows failed, if all pass it shows passed, and so on.

## Requirements

- **.NET SDK** with the `dotnet` CLI on PATH (or configure `csharpTestExplorer.dotnetPath`)
- Test projects must reference **`Microsoft.NET.Test.Sdk`** and a framework adapter (NUnit3TestAdapter, xunit.runner.visualstudio, MSTest.TestAdapter, etc.)
- For debugging, the editor must support `coreclr` attach (e.g. the C# extension or C# Dev Kit)

<!-- AUTO:SETTINGS:START — generated by scripts/update-readme.mjs, do not edit manually -->
## Extension Settings

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `csharpTestExplorer.dotnetPath` | `string` | `"dotnet"` | Path to the dotnet CLI executable |
| `csharpTestExplorer.testArguments` | `string` | `""` | Additional arguments to pass to dotnet test |
| `csharpTestExplorer.excludeProjects` | `string[]` | `[]` | Glob patterns for projects to exclude from test discovery |
| `csharpTestExplorer.buildBeforeTest` | `boolean` | `true` | Build the project before running tests |
| `csharpTestExplorer.autoDiscoverOnOpen` | `boolean` | `true` | Automatically discover tests when the workspace opens |
<!-- AUTO:SETTINGS:END -->

<!-- AUTO:COMMANDS:START — generated by scripts/update-readme.mjs, do not edit manually -->
## Commands

All commands are available from the **C# Tests** sidebar and the Command Palette (prefix `C# Test Explorer`).

| Command | Description |
|---------|-------------|
| Refresh Tests | Re-scan projects and re-discover all tests |
| Run All Tests | Run every discovered test |
| Run Test | Run the selected test, class, namespace, or project |
| Debug Test | Debug the selected test node with coreclr attach |
| Show Output | Open the C# Test Explorer output channel |
| Go to Test Source | Open the source file at the test method's line |
| Stop Test Run | Cancel the currently running test execution |
| Filter Tests | Filter the test tree by name (case-insensitive substring match) |
| Run Selected Tests | Run all currently selected tests in the tree |
| Clear Filter | Remove the active test filter and restore the full tree |
<!-- AUTO:COMMANDS:END -->

## How It Works

1. **Project detection** — Scans the workspace for `.csproj` files and inspects their `PackageReference` entries for known test framework packages (NUnit, xUnit, MSTest)
2. **Test discovery** — Parses `.cs` source files in each test project for test attributes (`[Test]`, `[Fact]`, `[TestMethod]`, `[TestCase]`, `[Theory]`, etc.) to extract fully-qualified test names and source locations
3. **Tree view** — Builds a hierarchical tree (Project → Namespace → Class → Method) displayed in a custom Activity Bar view
4. **Execution** — Runs `dotnet test` with an optional `--filter` and parses the resulting TRX XML for pass/fail/skip results
5. **Debugging** — Spawns `dotnet test` with `VSTEST_HOST_DEBUG=1`, monitors stdout for the test host PID, and attaches the `coreclr` debugger

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) for development setup, build instructions, and project architecture.

## License

MIT — see [LICENSE](LICENSE) for details.
